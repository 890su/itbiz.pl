---
import {
  getRoute,
  getRouteOrFallback,
  languageNames,
  locales,
  localeLabels,
  type Locale,
  type TranslationKey,
} from '../i18n/config';
import { getUi } from '../i18n/ui';
import { company } from '../data/company';

interface Props {
  variant: 'desktop' | 'mobile';
  locale: Locale;
  translationKey: TranslationKey;
}

const { variant, locale, translationKey } = Astro.props;
const copy = getUi(locale);
const navigation = (
  ['home', 'services', 'solutions', 'about', 'contact'] as TranslationKey[]
).map((key, index) => ({ href: getRoute(locale, key), label: copy.common.nav[index] }));
const currentPath = Astro.url.pathname;
const isCurrent = (href: string) =>
  href === getRoute(locale, 'home')
    ? currentPath === href
    : currentPath.startsWith(href);
---

{
  variant === 'desktop' ? (
    <aside class="side-panel" aria-label={copy.common.siteNav}>
      <a
        class="brand"
        href={getRoute(locale, 'home')}
        aria-label={copy.common.brandHome}
      >
        <span class="brand-node" aria-hidden="true" />
        <span>ITBIZ.PL</span>
      </a>
      <p class="side-kicker">
        {copy.common.sideKicker[0]}
        <br />
        {copy.common.sideKicker[1]}
      </p>
      <nav class="side-nav" aria-label={copy.common.mainNav}>
        <ul>
          {navigation.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isCurrent(item.href) ? 'page' : undefined}
              >
                <span>{item.label}</span>
                <span class="nav-port" aria-hidden="true" />
              </a>
            </li>
          ))}
        </ul>
      </nav>
      <div class="side-actions">
        <div class="utility-row" aria-label={copy.common.settings}>
          <nav class="locale-switcher" aria-label={copy.common.languageLabel}>
            {locales.map((targetLocale) => (
              <a
                href={getRouteOrFallback(targetLocale, translationKey)}
                lang={targetLocale}
                aria-current={targetLocale === locale ? 'page' : undefined}
                aria-label={languageNames[targetLocale]}
              >
                {localeLabels[targetLocale]}
              </a>
            ))}
          </nav>
          <button
            class="theme-toggle"
            type="button"
            data-theme-toggle
            aria-label={copy.common.theme}
          >
            <span aria-hidden="true">◐</span>
          </button>
        </div>
        <a class="menu-phone" href={company.publicPhoneHref}>
          <span>{copy.common.phoneAction}</span>
          <strong>{company.publicPhone}</strong>
        </a>
        <a class="button button-primary" href={getRoute(locale, 'contact')}>
          {copy.common.cta}
        </a>
      </div>
    </aside>
  ) : (
    <header class="mobile-bar">
      <a
        class="brand"
        href={getRoute(locale, 'home')}
        aria-label={copy.common.brandHome}
      >
        <span class="brand-node" aria-hidden="true" />
        <span>ITBIZ.PL</span>
      </a>
      <div class="mobile-actions">
        <button
          class="theme-toggle"
          type="button"
          data-theme-toggle
          aria-label={copy.common.theme}
        >
          <span aria-hidden="true">◐</span>
        </button>
        <button
          class="menu-toggle"
          type="button"
          data-menu-toggle
          aria-controls="mobile-menu"
          aria-expanded="false"
        >
          <span class="menu-label">{copy.common.menu}</span>
          <span class="menu-icon" aria-hidden="true" />
        </button>
      </div>
      <nav id="mobile-menu" class="mobile-menu" aria-label={copy.common.mainNav} hidden>
        <ul>
          {navigation.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isCurrent(item.href) ? 'page' : undefined}
              >
                {item.label}
              </a>
            </li>
          ))}
        </ul>
        <div class="mobile-language-block">
          <span class="mobile-utility-label">{copy.common.languageLabel}</span>
          <div
            class="mobile-locale-switcher"
            role="group"
            aria-label={copy.common.languageLabel}
          >
            {locales.map((targetLocale) => (
              <a
                href={getRouteOrFallback(targetLocale, translationKey)}
                lang={targetLocale}
                aria-label={languageNames[targetLocale]}
                aria-current={targetLocale === locale ? 'page' : undefined}
              >
                {localeLabels[targetLocale]}
              </a>
            ))}
          </div>
        </div>
        <a class="menu-phone" href={company.publicPhoneHref}>
          <span>{copy.common.phoneAction}</span>
          <strong>{company.publicPhone}</strong>
        </a>
        <a class="button button-primary" href={getRoute(locale, 'contact')}>
          {copy.common.cta}
        </a>
      </nav>
    </header>
  )
}
